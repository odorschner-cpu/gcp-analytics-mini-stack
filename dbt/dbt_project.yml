name: "mini_stack"
version: "1.0.0"
config-version: 2

profile: "mini_stack"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  mini_stack:
    staging:
      +materialized: view
    marts:
      +materialized: table